#include<stdio.h>
#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    char a[100];
    char adown[100];
    char aup[100];
    int numban;
    char ban[9]

    cin>>a;
    strcpy(aup,a);
    strcpy(adown,a);
    cin>>numban;

    for (int i=0; i<n; i++)cin>>ban[i];

    for (int i=0; i<strlen(a); i++)
    {
        for (int j=0; j<n; j++)
        {
            if(a[i] == ban[j])
            {
                if(aup[i] == '0') {}
            }
        }
    }
}
